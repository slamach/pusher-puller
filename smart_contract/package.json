{
  "name": "pusher-puller_smart-contract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "prepare": "cd .. && husky install smart_contract/.husky"
  },
  "lint-staged": {
    "*.{js,ts,json,sol}": "prettier --write"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.4",
    "hardhat": "^2.9.3",
    "husky": "^7.0.4",
    "lint-staged": "^12.4.0",
    "prettier-plugin-solidity": "^1.0.0-beta.19"
  }
}
